import { ArgsTable, Meta, Story, Canvas } from "@storybook/addon-docs";
import { Button, Props } from "../components/Button";

<Meta title="MDX/Button" component={Button} />

export const Template = (args) => <Button {...args} />;

# Button

let's enter a story for our `Button` component:

<Story name="My Button" args={{ children: "I am MDX" }}>
  {Template.bind({})}
</Story>

<ArgsTable story="My Button" />

<Canvas>
  <Story
    name="Secondary"
    args={{ children: "MDX Secondary", variant: "secondary" }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
